/*
import * as React from 'react';
import { Text, View, StyleSheet, Image } from 'react-native';

export default class AssetExample extends React.Component {
  render() {
    return (
      <View style={styles.container}>
        <Image source={require('../assets/sakura.png')} style={styles.backgroundImage} />
        <Text style={styles.paragraph}>
        {`
          1. Create a user input view.
          2. Show the result. Guess lower/greater.
          3. Create a gameover view.
        `}
        </Text>
        <Image style={styles.logo} source={require('../assets/1gohanchan.png')} />
      </View>
    );
  }
}

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
    padding: 24,
  },
  paragraph: {
    margin: 24,
    marginTop: 0,
    fontSize: 14,
    fontWeight: 'bold',
    textAlign: 'center',
  },
  logo: {
    height: 128,
    width: 128,
  },
  backgroundImage: {
    flex: 1
  }
});
*/